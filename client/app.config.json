{
  apps : [
    {
      name      : "client",
      script    : "npx",
      interpreter: "none",
      args: "serve ./build",
      instances: 1,
      autorestart: true,
      watch: false,
      max_memory_restart: '1G'
    }
  ]
}